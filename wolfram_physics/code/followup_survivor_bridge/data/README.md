This package does not introduce a new raw dataset.

It starts from the semantic quotient stored in:

- `code/ruliad_toy_benchmark_results.json`

That file is the input semantic quotient for the follow-up bridge theorems.
